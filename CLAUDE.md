# Answerable, agent guide

The full contract for agents driving this engine (CLI verbs, what publish does, the
outbox contract, guardrails) lives in [AGENTS.md](AGENTS.md), which is canonical. This
file exists only because some tools look for `CLAUDE.md` specifically; read AGENTS.md.
